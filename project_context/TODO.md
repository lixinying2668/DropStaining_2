# TODO.md

# 全自动冰冻切片染色机上位机待办清单

> 本文件以未完成工作为主，同时保留少量关键完成项用于追溯。Mock 业务闭环、正式数据库与 Web HMI 接入、历史 Mock 浏览器验收、数字孪生 XY 基线、主控/DCR55/制冷离线协议、离线 Real 读取边界、扫码器配置持久化基础、扫码区域配置表达、试剂坐标锚点生成、DCR55 控制服务抽象、样本/试剂扫码职责与坐标语义确认、SOCON SDK 静态兼容性取证、23D SOCON 独立 x86 Bridge 骨架、P0-01 SOCON 兼容性报告收口、P0-02 Bridge 正式 net452 x86 构建验证、坐标 Z 语义补齐、LiquidClass 参数补齐、工程手动移液测试 API、Workflow 通道关联模型评估及规划规则持久化均已进入提交基线。当前重点是修复最新完整回归、复核迁移与单页浏览器验收，并准备首次真实设备只读现场验收；当前 Bridge 正式构建/离线自检已完成。

## 当前真实只读接入准备（2026-07-20，HEAD `97d8539`）

### 已提交的软件基础

- [x] 7 月 17 日的 Workflow 规划规则、Mock 运行态重置、通道/玻片状态、DAB/预检/数字孪生运行衔接和相应浏览器验收修订已分批提交；`PersistWorkflowPlanningRules` 迁移已进入历史。
- [x] 主控只读边界已在 `b100217` 完善：白名单读取工作/节点状态及 0–3 板温控当前值、目标值和开关状态；真实串口按单请求打开/关闭，补齐关闭失败、超时、断连和脱敏诊断的离线验证。
- [x] SOCON Bridge 受门禁真实只读会话已在 `97d8539` 提交：双启用门禁、SDK新鲜部署校验、USB/端口/速率、NodeID白名单、代表节点和轴映射校验；支持会话打开、代表节点初始化/回零状态、X/Y/Z1/Z2静态位置读取和确认关闭。
- [x] 混匀参数、清洗阀配置、调试液体类型和工程模块测试前后端接线已提交；这些是数据库/Mock/工程软件能力，不代表真实外设动作可用。

### 当前必做验证与清理

- [ ] 修复当前完整后端回归：2026-07-20 实测 **313 项中 252 通过、61 失败、0 跳过**。优先处理种子数据增加后测试仍使用 `Single/Assert.Single` 的唯一性假设、`FactoryGeneral-v1` 与 `Ab` 同时匹配，以及已发布 Workflow 修改保护等契约差异；修复后必须重新记录完整汇总。
- [ ] 在隔离数据库验证 `PersistWorkflowPlanningRules`、串口连接、精度校准、混匀参数和清洗阀配置相关迁移，确认旧数据兼容且无 pending migration。
- [ ] 执行当前控制台/Workflow/时间线浏览器回归，确认最新配置、重置、扫码和运行接线没有破坏权限、审计、SignalR和Mock/Real边界。
- [ ] 核对主仓库未跟踪的 `tmp-bridge-build/`、`tmp-hardware-tests-build/`；将可再生产物清理或加入忽略规则，仅保留明确需要的脱敏验收证据。
- [x] 最新 SOCON Bridge 只读扩展已于 2026-07-20 正式构建：`Release|x86` **0 warning / 0 error**；从 `bin/x86/Release` 运行离线 `--self-test`，**93 checks passed**。本轮未配置 SDK、未连接硬件，不能替代 P1-01 SDK 部署验证或现场只读验收。

### 第一次真实设备测试计划

- [ ] 阶段 A 仅验证真实连接与只读：Bridge/SOCON单节点会话、初始化/回零状态、X/Y/Z1/Z2静态位置、主控工作/节点/温控状态、DCR55 串口打开/正常关闭生命周期（不发送扫码触发）、制冷温度和已确认安全 IO 静态快照。
- [ ] 阶段 A 全程禁止初始化、回零、运动、扫码触发、温控/制冷/PWM/混匀/液路写入、自动扫描、自动重试和自动重连。
- [ ] 阶段 A 通过且现场单独授权后，阶段 B 可对机械臂保持静止的 DCR55 执行受控单次扫码：有效测试码、无码/超时、CommandId幂等、失败后新命令恢复、断开重连后无旧缓存；禁止连续扫码、机械臂移动、ROI/校验光/软重启和正式样本绑定。
- [ ] 所有现场参数只保存在受控本机配置；现场报告使用 PASS / FAIL / BLOCKED / NOT_RUN，且不得记录真实COM、NodeID、SDK路径、坐标或患者/样本信息。

### 边界

- 当前完成的是首次真实只读测试的软件准备，不是现场验证通过。真实 COM、USB2CAN/CAN、DCR55、制冷和安全 IO 均尚未连接；机械臂回零和运动继续禁止。

## 最新完成项（2026-07-14，已提交）

### ✅ 本轮已实现并完成专项测试

- 数字孪生工程/配置页已将温控板启停与查询、混匀启停与查询、液体类型读取/保存/新建、扫码器创建与控制、液位阈值保存、样本清洗启停接到现有正式 API；不再使用“指令进入模拟队列”的占位行为。
- 新增 `POST /api/fluidics/wash-stop`：使用独立业务命令停止 PWM0 清洗泵，要求 admin 权限，支持命令幂等与审计，Real 模式 fail-closed。
- 新增 `POST /api/fluidics/level-thresholds`：更新 SystemWater / PBS / Waste / ToxicWaste 的低位/满位阈值，不修改容量和当前液量；阈值统一截断到 `0..CapacityUl`，要求 admin 权限、理由、命令幂等与审计，Real 模式 fail-closed。
- `FluidicsWashStopAndThresholdTests` 专项测试于 2026-07-14 验证 **4/4 通过**。

### 后续状态（已并入当前清单）

- [ ] 浏览器级回归已随控制台验收脚本演进，但当前 HEAD 的最新复核仍未完成；统一由本文件顶部“当前必做验证与清理”跟踪，不再以 7 月 14 日版本单独判定。
- [ ] 完整后端测试已于 2026-07-20 执行并发现 61 项失败；统一由本文件顶部回归修复项跟踪。
- [ ] 评估本次已提交的 `tmp-build/acceptance-build`、`tmp-build/final-review`、`tmp-build/review-build`、`tmp-build/acceptance-script.js` 和 `tmp-build/webcheck` 是否需要长期版本化；保留必要验收证据，并为纯临时构建产物完善 `.gitignore` 或仓库留存规则。
- [x] 该轮源代码、专项测试、浏览器验收脚本调整及验收产物已提交至 `cd52b22`；这是提交完成时的历史状态，不表示当前 HEAD 的完整回归通过或当前工作区干净。

### 边界

- 本轮只证明 Mock/工程控制台软件契约与专项测试通过；未连接真实 COM/CAN/TCP，未执行真实扫码、真实温控、真实混匀、真实液路或机械动作。
- 本轮已进入 Git 提交基线，但专项测试通过和代码提交不等于完整回归、真实硬件验收或正式发布验收完成。

## 已完成但保留追溯的扫码器后端事项

### ✅ 扫码器配置（已完成）

- **完成内容**：
  - 扫码器配置数据库化。
  - 扫码器配置持久化。
  - 扫码器配置管理接口。
- **边界说明**：本项仅代表配置管理能力完成；不代表真实设备参数已生效，也不代表数据库配置已经接管 DCR55 运行参数。

### ✅ 扫码区域（已完成）

- **完成内容**：
  - 扫码区域后端模型。
  - 样本区域扫码配置表达。
  - 区域编号、扫码顺序、坐标档案关联和坐标版本关联。
- **边界说明**：不代表扫码器配置页面、扫码区域配置页面或机械臂自动扫码流程完成。

### ✅ 坐标配置（已完成）

- **完成内容**：
  - 试剂坐标锚点。
  - 首尾坐标生成规则。
  - X / Y / Z 独立线性插值生成坐标，并使用既有 `CoordinateProfileVersion` 版本体系保存。
- **边界说明**：本项是坐标配置生成能力，不代表机械臂实际校准完成。

### ✅ DCR55（已完成）

- **完成内容**：
  - DCR55 控制服务抽象。
  - Restart / Calibration Light / ROI 后端接口。
- **边界说明**：本项是后端服务抽象和命令封装，不代表 DCR55 已完成真实接入、真实 ROI 效果验证或真实校验光效果验证。

### ✅ 扫码设备职责与坐标语义（已确认口径）

- **完成内容**：
  - DCR55 / `ArmCamera` 当前按机械臂末端样本扫码头理解，后续建议统一命名为 `DCR55Scanner` 或 `SampleScanner`。
  - `ArmCamera` / `DCR55Scanner` 是工具设备或工具参考，不应作为运动目标点。
  - 样本扫码由机械臂携带 DCR55 到对应 Slot / `ScannerRegion` 扫码目标位，静止后触发扫码。
  - 试剂扫码由主控板固定多通道扫码模块 `0x08` 负责，不使用 DCR55，也不使用 `SampleScan`。
- **边界说明**：本项仅代表语义口径确认；不代表代码命名、坐标模型、真实工具偏移、真实扫码路径或硬件联调已完成。

## 已完成但保留追溯的工程配置与 Workflow 事项

### ✅ 坐标 Z 语义补齐（已完成）

- **完成内容**：`CoordinatePoint.AspirateEndZUm`（Z-End）新增；Z 语义映射为 Z-Travel→`SafeZUm`、Z-Start→`LiquidDetectZUm`、Z-End→`AspirateEndZUm`、Z-Dispense→`DispenseZUm`；同步校准历史、坐标快照、校准请求与导入差异。
- **边界说明**：仅补齐配置表达与冻结快照；`AspirateEndZUm` 为 nullable、未纳入 Real readiness 门禁，旧坐标数据兼容；未接入 Motion 执行逻辑。

### ✅ LiquidClass 参数补齐（已完成）

- **完成内容**：`LiquidClassVersion` 新增 `LiquidFollowingDepthUm`、`RetractSpeedUmPerSecond`、`ConditioningVolumeUl`、`BreakoffSpeedUlPerSecond`、`PostDispenseAirGapUl`；System Trailing Air Gap 复用既有 `TrailingAirGapUl`；接入配置写入、校验、版本差异、冻结快照与读取。
- **边界说明**：仅补齐配置表达与快照；未接入执行。

### ✅ 工程手动移液测试 API（已完成）

- **完成内容**：`EngineeringPipettingService` 支持 LiquidDetect / Aspirate / Dispense / Wash / Flush；`Purge`、`ClearChannel` 列为 `UnsupportedOperations`；具备 engineer/admin 权限、EngineeringSession、CommandIdempotency、AuditLog，Real 模式 fail-closed。
- **边界说明**：未连接真实设备、未执行真实移液动作；不影响 Motion 正式执行、Workflow 执行、DCR55、SOCON、MainController。

### ✅ Workflow 通道关联模型（已完成评估）

- **历史记录保留**：Workflow 版本化、默认工作流、抗体映射、通道绑定、运行快照冻结等既有历史工作均保留追溯，不删除。
- **评估结论**：经代码评估，现有 `WorkflowDefinition` / `WorkflowVersion` / `WorkflowStep` / `WorkflowReagentRequirement` 与 `ChannelBatch.SelectedWorkflowVersionId` 已满足“实验流程加载”需求；同一 Channel 内 4 个玻片共享同一 Workflow、不同 Channel 可选不同 Workflow 已被数据模型与运行实例化强制；`StainingTask.WorkflowVersionId` 为任务意图/兼容校验，`ChannelBatch.SelectedWorkflowVersionId` 为运行执行权威。
- **结论**：无需新增一套 Workflow 定义/版本/通道关联模型；后续已为规划约束新增并提交 `WorkflowVersion.PlanningRulesJson` 和数据库迁移，因此“无需任何数据库字段或 Migration”的旧结论已失效。该扩展仍需按本文件顶部清单完成迁移验证和失败回归修复。

### 仍需完成（与上述配置能力配套）

- 前端工程配置页面：LiquidClass 主要参数、坐标 Z-Travel/Z-Start/Z-End/Z-Dispense 的读写，以及 LiquidDetect/Aspirate/Dispense/Wash/Flush 工程移液测试均已在 `/control-console` 接线；仍需修复当前后端回归并重跑浏览器验收，Real 动作继续 fail-closed。
- DCR55 真实硬件验证（见 P1-07）、主控真实硬件验证（见 P1-03）、SOCON 真实接入（见 P1-02）仍为未完成。
- 样本扫码坐标落地（DCR55 工具偏移、Slot / `ScannerRegion` 扫码目标位、`SampleScan` 去留 / 改名）仍为未完成。
- 试剂扫码通道映射（主控 `0x08` 的 `ch1–ch5` 与列 / R 位映射、EMPTY / INVALID 原始格式）仍为未完成。

## Workflow 执行 / 调度能力现状

- **已实现**：Mock `MachineExecutor`、运行状态机、`WorkflowStepExecution`/运行账本、资源租约，以及暂停、恢复、重做和故障处理基础能力已进入提交基线。
- **当前状态**：能力并非“未开始”，但当前完整后端回归仅 252/313 通过、61 失败；多项失败与新增种子数据后测试仍假定唯一记录有关，也存在发布 Workflow 修改冲突等契约偏差，须先恢复绿色基线。
- **仍需完成**：修复完整回归、复核迁移并重跑 `/control-console` 浏览器验收；真实 Motion、液路、温控与设备动作编排仍属于 Real 接入任务，不因 Mock 执行器存在而放行。
- **边界**：配置与执行分离；不绕过资源锁、命令幂等、审计、运行快照和 Real 安全门禁。

## P0：真实设备接入前的阻塞项

### ✅ P0-01｜SOCON 兼容性报告收口并提交（已完成）

- **完成时间**：2026-07-08 只读核对确认。
- **完成证据**：
  - `real-hardware/socon-sdk-compatibility-report.md` 已存在并已提交至 HEAD（`e7c127df`）。
  - 工作区无 `socon-sdk-compatibility-report.md` 的后续改动（`git status --short` 未列出该文件）。
  - 报告不含本机绝对路径、厂商 DLL、Demo、驱动、授权文件或真实设备凭据。
- **说明**：本条目已完成，仅保留作为追溯记录。

### ✅ P0-02｜补齐 Bridge 正式 net452 x86 构建验证（已完成）

- **完成时间**：2026-07-08 只读核对确认。
- **完成证据**：
  - VS2022 MSBuild `Release|x86` 正式构建 `bridges/Stainer.SoconBridge/Stainer.SoconBridge.csproj` 成功。
  - 目标框架保持 `.NET Framework 4.5.2`，平台目标保持 `<PlatformTarget>x86</PlatformTarget>`。
  - 正式构建输出 `bin\x86\Release\Stainer.SoconBridge.exe`。
  - 使用正式构建输出运行 Bridge `--self-test`，结果为 **59 checks passed**。
- **边界说明（重要）**：
  - 本次验证未加载 SOCON SDK、未连接真实硬件、未扫描 USB/COM/CAN/网络设备。
  - 本次验证基于 HEAD（`e7c127df`）的 Bridge 代码，不包含工作区后续 4 个 Bridge 源码改动（`ReflectionOnlyManagedAssemblyLoadProbe` 相关诊断能力属于 P1-01，见下）。
- **说明**：本条目已完成，仅保留作为追溯记录。

### P0-03｜完成真实设备现场接口、映射与安全 IO 取证

- **目标**：将真实硬件接入所需事实转化为经现场确认、可审计的配置与接口资料，消除 NodeID、接线、端口、映射和安全状态的不确定性。
- **当前状态**：主控、DCR55、制冷和 SOCON 已有离线协议/SDK资料；`real-hardware/真实设备接口参数清单.md` 已整理 P0-03 现场取证清单（已确认资料级事实 / 待确认现场参数 / 安全 IO 取证矩阵 / 各设备取证清单 / 阻塞关系表 / 现场取证方式 / 不得入库敏感参数清单）。**真实设备连接、接线、NodeID、设备映射与安全 IO 仍未现场确认**，P0-03 当前仍为「待现场确认 / 未完成」。
- **重要边界**：P1-01（SDK 运行时部署验证）通过**不等于**真实设备已连接、可通讯、可只读或可运动；P1-02 仍须等待 P0-03 / P0-04 / P0-05 门禁，不得提前放行。
- **前置条件**：可接触真实设备、厂商工程人员和现场资料；仅允许只读核对。
- **完成标准**：
  - 确认主控实际通讯方式、固件、端口识别、ACK、超时、断连与重连规则。
  - 确认 DCR55 的命令终止符、无码/多码返回、触发时序、超时和错误行为。
  - 确认制冷实际接线及控制通道（主控接口或独立 RS485）、ACK、稳定判定、报警与超时规则。
  - 确认 USB2CAN 型号、驱动、SOCON NodeID、设备类型、轴/泵/SOCA/SOPA 映射、每毫米参数、坐标方向和单位。
  - 建立真实安全 IO 矩阵：急停、门禁、抽屉、限位、碰撞、水空、PBS 空、废液满、有毒废液满、温控/制冷异常、通讯断连等；每项包含来源、极性、刷新策略、断线策略和联锁动作。
  - 现场敏感参数仅保存于受控本机/工程配置，不提交 Git。
  - 取证清单中所有「待确认」项在现场核对后更新为「已确认」，且仅记录取证结论、不记录真实敏感值。
- **建议下一步**：现场取证按 `real-hardware/真实设备接口参数清单.md` 第 9 节「现场取证方式」逐项只读核对；所有未确认字段保持「待确认」，不得以 SDK 默认值、Demo 配置、历史样例或推测值替代。

### P0-04｜定义并评审真实运行安全门禁、异常处置与恢复规则

- **目标**：将真实设备运行前置门禁、异常停止、温控/制冷阈值、通讯异常和断电恢复规则固化为正式安全策略。
- **当前状态**：**规则草案编制中 / 待评审 / 未完成**。Mock 层已有基础门禁；真实安全输入、稳定阈值、过温/超时和断电恢复策略尚未确认。P0-04 草案正在形成，仍需现场确认与安全评审。已产出 `real-hardware/真实运行安全门禁与异常恢复规则.md`（草案 v0.1），覆盖真实动作前总门禁、安全 IO 异常规则矩阵、通讯异常规则、运行中断与恢复规则、温控/制冷安全规则、真实能力分级门禁、P1 阶段阻塞关系、待现场确认清单；所有未现场确认内容标注「待现场确认」，未含任何真实敏感现场参数。**该草案完成不代表真实安全 IO 已接入，也不代表可进入 P1-02**；P0-04 仍未完成，须待工程评审与现场取证补齐后再定稿。
- **前置条件**：P0-03 完成；厂商/现场工程人员确认设备安全行为。
- **完成标准**：
  - 明确真实运行阻断条件及对应报警、停止、人工确认与恢复规则。
  - 明确温控稳定范围/持续时间、升温超时、过温、传感器故障和加热板故障规则。
  - 明确制冷目标偏差、稳定超时、异常、断连和恢复规则。
  - 明确中断、断电、重启后的运行状态、针内液体、DAB、库存预约、Pending 通讯记录及人工处置规则。
  - 策略进入正式安全文档、报警模型和 Real 接入实现计划；未满足门禁时保持 fail-closed。
- **建议下一步**：先评审“异常停止后如何安全保持、谁可确认、从哪里恢复”状态机，再开放任何真实写操作。

### P0-05｜补齐真实运动坐标参数并建立现场校准方案

- **目标**：在已导入的 DigitalTwinXY-v1 基线上补齐真实运动所需 Z、路径、速度和软限位，并形成可执行的校准流程。
- **当前状态**：**校准方案草案编制中 / 待评审 / 未完成**。已产出 `real-hardware/真实运动坐标参数与现场校准方案.md`（草案 v0.1），覆盖 DigitalTwinXY-v1 基线边界、坐标单位与换算、X/Y/Z/Z1/Z2 映射、目标点参数、双针 25 mm 名义偏移验证、各目标区域、回零与单轴、无液低速空跑、现场 SOP、版本/冻结/回退/审计、P1-08 门禁和待现场确认清单。当前仅有未验证的 XY 数字孪生基线；真实 Z、高度、速度、加速度、软限位、轴映射和现场校准值均未确认。**草案不代表真实坐标已校准，不代表机械臂可运动，不放行 P1-08**。
- **前置条件**：P0-03 确认轴映射、方向与单位；P0-04 安全门禁明确；现场允许受控无液低速校准。
- **完成标准**：
  - 每个可执行点具备工程受控配置：X/Y/Z、安全高度、接近/离开高度、探液/吸液/加液高度、速度、加速度、软限位及校准状态。
  - 验证双针实际间距、碰撞约束、试剂架、玻片位、洗针位、DAB 位和混合区位置。
  - 建立坐标校准、复核、发布、冻结、回退和变更审计流程。
  - 未校准/未验证坐标继续在 Real 模式下阻断真实动作。
- **建议下一步**：先完成草案评审并补齐 P0-03 / P0-04 相关现场门禁；现场条件和独立授权均满足后，才可另行按“回零→单轴→XY→双针→目标点”顺序申请验证，当前不得执行。

## P1：真实设备接入与实验闭环

### ✅ P1-01｜验证 SOCON SDK 运行时部署并保持进程隔离（已完成 / ready-to-close）

- **完成时间**：2026-07-08 最终有效验证轮通过。
- **验证方式**：由执行进程在自身 PowerShell 会话内设置进程级环境变量 `STAINER_SOCON_SDK_DIR` 指向本机受控 SDK 部署目录，并在同一会话内继承给 Rebuild 与 `--self-test` 子进程；全程未连接硬件、未扫描 USB/COM/CAN、未打开端口、未实例化 SDK 类型、未调用任何真实动作方法。报告与文档均不写入 SDK 绝对路径。
- **完成证据（运行时部署验证）**：
  - `SdkPathConfigured = configured`；`SdkDirectoryExists = yes`；SDK 来源仅记录为环境变量名 `STAINER_SOCON_SDK_DIR`（不含绝对路径）。
  - 核心文件齐：`SOCON.API.dll`、`SOCON.Utility.dll`、`can_bootloader.dll` 均 exists。
  - `can_bootloader.dll` PE Machine = `x86 (0x014C)`，与 Bridge x86 目标不冲突。
  - SOCON.API / SOCON.Utility 托管元数据加载 = `PASS`（`Assembly.ReflectionOnlyLoadFrom()` 仅反射上下文）。
  - 必要类型 5/5 available：`SOCON.API.SCDevice`、`SOCON.API.SCDeviceMA`、`SOCON.API.Utility+e_ConnectType`、`SOCON.API.Utility+DeviceTypeEnum`、`SOCON.API.Utility+ProtocolTypeEnum`。
  - `--self-test`：**60 checks passed**，`Result: PASS`，无异常。
  - 无路径泄露、无越界行为；git 工作区验证前后干净。
- **Optional dependency warning（非阻塞，交人工判断）**：
  - `SOCON.ScEventBus.dll` missing
  - `C1.C1Zip.4.dll` missing
  - 当前不阻塞 P1-01；后续进入真实连接/动作前（即 P1-02 起）继续评估是否需要补齐。
- **边界限定（重要）**：
  - P1-01 通过 **不等于** 真实机械臂已连接或可用。
  - 未连接真实硬件；未打开 COM/CAN/TCP；未实例化 SDK 类型；未调用 `Connect`/`OpenPort`/`Init`/`Move`/`Aspirate`/`Dispense`/`WaitActionDone` 等任何真实动作方法。
  - `--self-test` 中出现的 Connect/OpenPort/Move 等仅为 IPC 命令路由层演练，未触达 SDK 真实实现。
  - P1-02（真实连接与只读状态）仍需单独启动，并经过只读/连接边界审批；P1-01 关闭不自动放行后续真实通讯。
  - `ReflectionOnlyLoadFrom()` 虽不执行设备动作，但会把 SOCON 托管 DLL 加载到 Bridge 进程地址空间；本次验证确认其运行时依赖解析未产生意外副作用，但仍不得据此写成真实设备可用。
- **说明**：本条目已完成，仅保留作为追溯记录。

### P1-02｜接入 SOCON/USB2CAN 的真实连接与只读状态

- **目标**：在 Bridge 内建立真实 SOCON 连接，只读取设备通讯、初始化、回零、位置和错误状态。
- **当前状态**：**受门禁只读软件实现已提交，真实硬件验证未开始**。`97d8539` 已增加 `OpenConfiguredReadOnlySession`、`GetConfiguredNodeBasicStatus`、`GetConfiguredAxisPositions`、`CloseConfiguredReadOnlySession`，并使用双启用、SDK部署、USB连接类型、NodeID白名单、代表节点和轴映射门禁；真实USB2CAN、NodeID、初始化/回零状态和位置尚未现场读取。
- **前置条件**：P0 全部完成；P1-01 完成；现场允许只读测试；设备处于无液、无样本、无障碍安全状态。
- **完成标准**：
  - 仅在显式工程授权与安全门禁满足时连接。
  - 可读取连接、节点通讯、初始化、回零、X/Y/Z、SDK 错误摘要和安全相关状态。
  - Stainer 主进程仅通过受控 Named Pipe 客户端获取结构化只读结果，不得直接引用 SOCON SDK。
  - 建立 Bridge 进程可用性、IPC 超时、断连、异常退出和受控重连/人工恢复边界；不得因 Bridge 重启自动恢复或重复发送任何物理动作。
  - 未授权、未知、断连或异常时 fail-closed。
  - 不允许回零、运动、液面检测、吸液、加液或泵动作。
  - 完成真实只读验收记录。
- **建议下一步**：先完成最新Bridge正式x86构建/自检和现场配置审阅，再在明确授权下只连接一个已确认节点，读取初始化/回零状态和静态位置后正常关闭；本轮不得初始化、回零或运动。

### P1-03｜接入主控、DCR55 和制冷设备的真实只读通讯

- **目标**：将现有离线协议解析接入真实 Transport，实现状态与扫码结果的可靠读取。
- **当前状态**：**进行中**。离线协议与读取边界完成；真实端口未开启。
  - **主控已完成**：真实通讯 Transport 软件基础设施、白名单只读边界和离线验证；`b100217` 已补齐工作/节点状态、0–3板温控当前值/目标值/开关状态、单请求串口打开/关闭和关闭失败诊断。
  - **主控剩余**：真实 COM 连接验证、真实主控状态读取、温度状态读取、IO 状态读取、DeviceHealth 映射及后续真实硬件验证。
  - **DCR55**：Serial Transport 代码已完成，已支持受控单次触发通讯链路；等待现场 COM 与设备授权后执行真实单次扫码验证。
- **前置条件**：P0-03 完成；实际端口、终止符、超时和错误行为已确认；现场允许只读通讯。
- **完成标准**：
  - 主控可读取经确认的工作/节点/温控/光电等状态，并正确处理 CRC、分包、拼包、超时和断连。
  - DCR55 可读取真实单码、多码、无码和超时结果；未确认前不自动触发物理扫码。
  - 制冷可读取真实温度并识别异常帧、断连和超时。
  - 读取错误进入正式报警与工程诊断。
  - 不开放真实写命令。
- **建议下一步**：按主控、DCR55、制冷分别完成单设备验收后再汇总到统一设备健康状态。

### P1-04｜接入真实安全 IO、设备健康监控与硬件联锁

- **目标**：将经现场确认的急停、门禁、抽屉、限位、碰撞、液位、温控/制冷异常和通讯状态接入正式安全服务、报警与执行门禁。
- **当前状态**：安全规则和部分协议资料已有；真实输入读取、断线默认策略、正式报警联动和执行器联锁未实现。
- **前置条件**：P0-03、P0-04 完成；P1-03 已可读取主控状态；能获得机械臂/CAN 和其他安全输入的真实状态。
- **完成标准**：
  - 每个安全信号使用已确认来源、极性、刷新或主动上报规则；未知或断线默认按不安全处理。
  - 急停与普通停止明确区分；急停、门禁、限位、碰撞和关键液位异常能立即阻止后续真实动作。
  - 安全状态进入正式报警、审计、SignalR 和工程诊断，不依赖浏览器状态。
  - 运行中异常不会自动恢复；解除联锁需受权限、二次确认和状态复核控制。
  - 可用可控测试或现场安全测试验证每类信号的正常、异常、断线和恢复边界。
- **建议下一步**：先完成“安全输入—报警—执行器阻断—人工复核”闭环，再开放任何真实写操作。

### P1-05｜实现真实设备启动、自检、状态回收与运行前对账

- **目标**：在 Real 模式下建立“设备启动后先读取、再对账、后放行”的安全启动流程，避免数据库状态与真实设备状态不一致时继续运行或重复执行动作。
- **当前状态**：Mock 运行恢复、通信 Pending 记录和数据库追溯基础已完成；真实设备启动、自检、设备状态回收与 SQLite 对账尚未实现。
- **前置条件**：P0-04 完成；P1-02、P1-03、P1-04 已具备真实只读和安全状态；现场已确认可读取的机械臂、温控、制冷、液路和扫码状态。
- **完成标准**：
  - Real 模式启动后默认保持不可运行，先获得设备健康和关键状态快照。
  - 对账正式数据库中的运行批次、设备通信 Pending、针内液体状态、DAB 生命周期、库存预约、温控/制冷状态与真实设备可读取状态。
  - 对任何无法确认、设备与数据库不一致、通信中断或历史 Pending 状态，标记为需人工工程处置；不得自动补发、自动继续或自动释放资源。
  - 输出明确的启动前预检/工程诊断结果，并区分“可运行”“仅只读”“需人工处置”“禁止运行”。
  - 完成后才允许进入受控真实写入或真实运动阶段。
- **建议下一步**：先建立“数据库状态 × 真实设备状态 × 允许操作”的对账矩阵，再实现启动状态机和工程处置界面。

### P1-06｜受控开放温控、制冷、液路、混合与排废相关真实写入

- **目标**：在安全门禁和只读验证之后，逐项开放低风险真实写操作，并建立状态回读和异常停止闭环。
- **当前状态**：真实写操作全部 fail-closed。
- **前置条件**：P0-04、P1-03、P1-04、P1-05 完成；真实安全 IO 可读取；无样本、无真实试剂或使用安全替代液。
- **完成标准**：
  - 每项写操作具备显式授权、CommandId 幂等、Pending/Complete/Failed/Unknown 持久化、审计、超时/断连处理与状态回读。
  - 温控实现稳定、超时、过温和异常停止逻辑。
  - 制冷实现启动/停止/目标温度、稳定与异常逻辑。
  - PWM、混合、清洗相关液路、排水/排毒泵及其他经确认 IO 动作均具备最小安全范围、停止策略和异常锁定。
  - 不因失败、数据库异常或前端重试自动重复发送真实物理动作。
- **建议下一步**：按“单设备、单命令、无液、低风险、回读确认”顺序逐项启用。

### P1-07｜实现真实扫码触发与样本/试剂扫码闭环

- **目标**：在已确认现场协议和只读通讯后，受控开放主控二维码与 DCR55 的物理扫码触发，并接入现有样本、试剂、LIS 和有效性校验流程。
- **当前状态**：主控二维码与 DCR55 的协议解析、离线读取边界和 Mock 业务流程已完成；ScannerProfile / ScannerRegion 配置持久化与配置管理 API、样本区域扫码配置表达、试剂坐标锚点生成、DCR55 Restart / Calibration Light / ROI 后端接口已完成；DCR55 负责样本 / 玻片码、主控 `0x08` 固定模块负责试剂码的职责口径已确认；真实触发、停止、复位、真实控制效果验证与实际扫码闭环未实施。
- **前置条件**：P0-03、P0-04、P0-05、P1-03、P1-04、P1-05 完成；已确认各扫码器的物理位置、DCR55 工具偏移、Slot / `ScannerRegion` 扫码目标位、命令终止符、触发时序、EMPTY/INVALID 的原始规则、无码/多码规则、超时策略、主控 `0x08` 通道与 R 位映射。
- **仍需保留的未完成项**：
  - DCR55 真实串口控制验证。
  - ROI 命令现场验证。
  - 校验光现场验证。
  - 真实扫码触发与样本/试剂业务闭环。
  - 数据库配置接管 DCR55 运行参数。
  - 扫码器配置页面。
  - 扫码区域配置页面。
  - DCR55 扫码中心相对机械臂坐标基准 / Needle1 基准的工具偏移标定。
  - 各 Slot / `ScannerRegion` 样本扫码目标位、静止姿态和安全路径确认。
  - `SampleScan` 是否从必需运动目标中移除，或改名为 `SampleScannerCalibrationPoint` 作为校准 / 测试 / 参考点保留。
  - 主控 `0x08` 的 `ch1–ch5` 与试剂架列、列式位置标记、既有 `R1–R40` 业务编号的统一映射。
  - 主控 `0x08` 的 EMPTY / INVALID 原始返回格式和超时行为。
- **完成标准**：
  - 仅在显式授权、设备健康和对应页面/流程预检通过时，发送经现场确认的扫码开始、停止或复位命令。
  - 主控二维码与 DCR55 各自使用明确状态机，避免重复触发、并发冲突、无限重试或错误设备读取。
  - DCR55 仅用于样本 / 玻片码；主控 `0x08` 固定扫码模块仅用于试剂瓶码；`SampleScan` 不作为试剂扫码位置。
  - 单码、多码、EMPTY、INVALID、无码、超时、断连和重复码均进入正式业务校验、报警、审计和用户提示。
  - 扫码命令具备 CommandId、Pending/Complete/Failed/Unknown 通讯记录；失败时不得盲目重发物理触发命令。
  - 扫码结果正确进入既有样本确认、试剂架、试剂有效性和 LIS 流程；不将设备绝对路径、端口或敏感配置暴露给操作员。
- **建议下一步**：先完成 DCR55 工具偏移、单个 Slot / `ScannerRegion` 样本扫码位和主控 `0x08` 单列试剂扫码映射取证，再以单次扫码、测试码验证触发—返回—业务校验闭环。

### P1-08｜受控开放 SOCON 回零、低速运动与双针空跑

- **目标**：在真实安全 IO 和坐标校准完成后，验证回零、低速运动、双针几何和目标点空跑。
- **当前状态**：未实施。
- **前置条件**：P0-05、P1-02、P1-04、P1-05、P1-06 完成；现场安全评估通过；无玻片、无试剂、无样本、无液体。
- **完成标准**：
  - 回零和移动均需显式授权、冻结坐标快照、安全 IO、软限位、超时和审计。
  - 完成单轴低速、XY、双针间距/碰撞、试剂架、洗针、玻片位和 DAB 位空跑验证。
  - 位置/限位/碰撞/通讯异常时立即停止后续动作并保留可追溯状态。
  - 形成机械臂空跑验收记录。
- **建议下一步**：先验证单轴最低风险路径，再进入双针与多目标点空跑。

### P1-09｜校准真实 Liquid Class、液量计量、针路排空与洗针效果

- **目标**：将现有 Liquid Class 配置和双针状态模型转换为经实际液路验证的体积、速度、预润湿、排空、回吸和洗针参数。
- **当前状态**：Liquid Class 版本化、快照和 Mock 行为已完成；真实吸液/加液体积精度、针路残留、交叉污染风险和洗针效果未验证。
- **前置条件**：P1-06、P1-08 完成；具备经批准的替代液、量测方法、废液处理和安全空跑条件。
- **完成标准**：
  - 对每类液体/工艺动作建立可复现的体积准确度、重复性、速度、预润湿、回吸、排空和洗针参数验证记录。
  - 验证 Needle1/Needle2 的独立液路、换液清洗、残留、交叉污染和异常空吸/堵塞处理边界。
  - 校准结果以受控 Liquid Class 版本发布，并在运行时冻结快照；不得直接改写已发布版本。
  - 任何未通过计量或清洗验证的液类不得用于真实 HE/IHC。
- **建议下一步**：先用水或批准替代液完成单针单体积重复性测试，再扩展到双针换液和洗针验证。

### P1-10｜将已验证 Real Adapter 与 Bridge 正式接入现有执行器

- **目标**：在不绕过现有业务、资源锁、命令幂等、审计和运行快照的前提下，把已逐项验证的真实设备能力接入正式 Real 模式执行链路。
- **当前状态**：现有执行器、资源锁、运行账本和 Mock 适配器已完成；Real 读取边界和 Bridge 尚未接入正式动作编排。
- **前置条件**：P1-05 至 P1-09 中与目标流程相关的能力已通过；每项真实命令、状态回读和安全门禁均已单独验收。
- **完成标准**：
  - Stainer 主进程只经正式 Device Adapter / Bridge Client 调用真实能力；不直接引用 SDK 或绕过应用服务。
  - 真实动作仍由现有运行状态机、资源锁、CommandId、设备通信持久化、运行账本、审计和配置快照管理。
  - 双针并行仅在几何、安全、液路、目标兼容与资源锁均通过时允许；否则保守串行。
  - 设备/Bridge 断连、未知结果、数据库异常和执行器重启均保持 fail-closed，禁止自动补发或自动继续。
  - 在隔离环境完成一条最小真实动作链路的集成验证，并形成回滚/禁用开关。
- **建议下一步**：先接入一个已验证的低风险只读/写入组合，再逐步接入空跑动作，不一次性替换 Mock 执行链路。

### P1-11｜接入实际 LIS

- **目标**：以可追溯、可失败、可降级的方式替换 LIS Mock。
- **当前状态**：仅有 Mock；实际数据源、数据字典、认证和多结果规则未知。
- **前置条件**：获得 LIS 方脱敏测试资料、接口/数据库说明和权限许可；完成隐私与安全审查。
- **完成标准**：
  - 明确数据源、字段、样本号规则、多结果规则、编码、认证、超时和错误处理。
  - 正式 LIS Adapter 不破坏 Mock 模式。
  - 查询成功、无结果、多结果、字段异常和连接失败均有正式 UI、审计和可理解错误。
  - LIS 地址、凭据和敏感信息不进入 Git。
  - 脱敏测试数据验证通过。
- **建议下一步**：先向 LIS 方索取数据字典和脱敏测试样本，再确定接口边界。

### P1-12｜完成 HE 真实空跑、替代液验证与湿实验

- **目标**：验证 HE 的真实设备端到端闭环。
- **当前状态**：Mock HE 已验收；真实 HE 未开始。
- **前置条件**：P1-05 至 P1-10 完成；涉及样本/试剂扫码时 P1-07 完成；实验耗材、废液和安全流程就绪；现场批准实验。
- **完成标准**：
  - 依次通过：无玻片无液空跑、有玻片无液空跑、替代液液路验证、替代液 HE 流程、单通道 HE 湿实验、多玻片 HE 湿实验。
  - 每一步验证样本/试剂扫码、试剂消耗、玻片位、温控、制冷、双针、洗针、废液、报警、暂停/停止和审计追溯。
  - 形成正式 HE 验收记录与异常清单。
- **建议下一步**：先做 HE 无液空跑与替代液验证，不直接进入真实染色。

### P1-13｜完成 IHC/DAB 真实替代液验证与湿实验

- **目标**：验证 IHC 通道规则、一抗码、DAB 生命周期和真实设备执行闭环。
- **当前状态**：Mock IHC/DAB 已验收；真实 IHC 未开始。
- **前置条件**：P1-12 稳定通过；DAB 真实替代液验证准备完成；一抗映射和试剂供应确认。
- **完成标准**：
  - 通道级默认 IHC 工作流、一抗码和批次规则正确。
  - DAB 配比、M1–M8 占用、三小时有效期、重配、清洗和耗材追溯正确。
  - 依次通过 DAB 替代液专项、IHC 全流程替代液、单通道 IHC 湿实验、多玻片 IHC 湿实验。
  - 异常、暂停、恢复、库存、DAB 和审计均可追溯。
- **建议下一步**：先完成 DAB 替代液专项验证，再开展完整 IHC 流程。

## P2：生产部署、性能与长期运维

### P2-01｜验证真实设备性能目标

- **目标**：在不降低安全与可追溯性的前提下，验证真实流程的节拍目标。
- **当前状态**：未验证；Mock 时序不能作为真实性能结论。
- **前置条件**：P1-12、P1-13 稳定通过；建立标准化样本与计时口径。
- **完成标准**：
  - 验证并记录单张玻片 `≤10 分钟`、十六张玻片 `≤15 分钟` 的实际表现。
  - 记录步骤耗时、并行情况、等待、瓶颈、异常率和重复性。
  - 性能优化不绕过洗针、温控稳定、安全门禁或审计。
  - 未达标时形成原因和优化计划。
- **建议下一步**：从单通道单玻片 HE 开始，逐步测量多通道与十六玻片满载。

### P2-02｜完成 Windows Kiosk、自动启动与现场部署验证

- **目标**：使系统在现场 Windows 工控环境可受控、稳定地长期运行。
- **当前状态**：正式 Host 与 Web HMI 已可运行；Kiosk、自动启动、服务恢复和现场权限模型未验证。
- **前置条件**：至少完成 HE 真实流程验证；明确现场账户、网络和维护方式。
- **完成标准**：
  - 验证开机启动、应用启动顺序、Kiosk/全屏、设备未就绪 fail-closed、日志保留和工程维护入口。
  - 操作员无法绕过 UI 进入工程调试或底层系统配置。
  - 明确断网下本地运行边界与恢复策略。
- **建议下一步**：先在独立测试 Windows 环境验证自动启动、崩溃重启和 Kiosk 退出恢复。

### P2-03｜完成备份、恢复、断电与运行中断演练

- **目标**：验证数据库、运行记录和设备状态在现场异常情况下可恢复、可追溯且不产生危险重复动作。
- **当前状态**：备份机制和基础恢复验证已完成；正式恢复、断电和运行中断演练未完成。
- **前置条件**：已有真实运行记录；已确定备份保留策略和现场存储方案。
- **完成标准**：
  - 演练定期备份、完整性检查、恢复、WAL 场景、磁盘空间不足和备份清理。
  - 演练运行中断后 DAB、针内液体、库存预约、Pending 通讯和批次状态的人工处置。
  - 不通过删除 `.db-wal` 或 `.db-shm` 解决运行问题。
  - 形成恢复 SOP 与演练记录。
- **建议下一步**：先用正式数据库副本进行完整恢复演练，再设计断电和中断场景。

### P2-04｜完成 Real 模式端到端回归、权限与发布验收

- **目标**：在真实设备 HE/IHC 流程稳定后，对正式 Web HMI、Real Adapter、Bridge、权限、审计、报警、运行追溯和部署边界进行发布前验收。
- **当前状态**：Mock 模式曾完成阶段性浏览器验收和后端回归；当前 HEAD 后端完整回归为 252/313 通过、61 失败，最新浏览器回归仍待复核；Real 模式端到端发布验收尚未开始。
- **前置条件**：P1-12、P1-13 完成；现场硬件、桥接进程和正式部署环境稳定。
- **完成标准**：
  - 在正式或隔离验收环境执行后端、数据库、Bridge、Web HMI 和真实设备链路回归。
  - 覆盖 HE/IHC、样本/试剂扫码、DAB、预检、启动、暂停、停止、异常、恢复、权限、审计、报警和历史追溯。
  - 验证操作员界面不暴露内部路径、原始通信、SDK 细节或工程诊断信息；工程权限受二次认证与审计约束。
  - 验证生产环境不暴露已限制的旧 Mock/legacy API 与控制台。
  - 对所有未通过项形成发布阻断清单，全部关闭或获得书面风险接受后才进入正式上线。
- **建议下一步**：在 HE/IHC 真实验证稳定后，制定一份独立的 Real 模式验收用例清单，避免沿用 Mock 结果作为上线依据。

### P2-05｜完善现场 SOP、工程维护和验收文档

- **目标**：形成可交付、可培训、可维护的现场操作与工程支持体系。
- **当前状态**：已有 Mock、诊断和 HE 干跑相关文档；真实设备、校准、恢复与维护 SOP 未完成。
- **前置条件**：至少完成 HE 真实流程验证；现场接口和报警策略最终确认。
- **完成标准**：
  - 完成操作员、HE、IHC、DAB、报警、工程诊断、坐标校准、启动/停机、断电恢复、数据库恢复、试剂/废液维护、设备通讯异常和版本变更 SOP。
  - 完成现场验收报告模板、配置版本记录模板和异常处理记录模板。
  - 所有 SOP 与实际已验证行为一致，不把 Mock 能力描述为真实设备能力。
- **建议下一步**：优先完成“异常停止→安全检查→工程处置→恢复运行”SOP，再补正常操作流程。

### P2-06｜完成最终触屏 UI 视觉重构与现场可用性验收

- **目标**：在保持正式 API、DTO、SignalR、权限和业务权威不变的前提下，完成面向触屏操作的最终视觉与交互重构。
- **当前状态**：操作员、工程、管理、历史和报警页面已接入正式数据；当前 UI 以流程正确性为优先，尚非最终视觉方案。
- **前置条件**：P1 HE/IHC 真实流程至少稳定通过；核心页面、报警和工程权限规则不再频繁变动。
- **完成标准**：
  - 以正式 API 和正式读模型为唯一数据来源，不恢复旧 Mock/legacy 页面或接口依赖。
  - 在目标分辨率与触屏环境验证 `/control-console` 内的总览、样本、试剂、DAB、运行、报警、历史、工程和管理视图。
  - 明确运行中高风险操作、预检、停止、报警确认和工程入口的视觉层级与误触防护。
  - 操作员界面持续隐藏内部路径、通信、SDK 和工程诊断细节；工程界面保留权限和审计。
  - 浏览器级验收覆盖关键交互、SignalR 重连、双窗口同步、1920×1080 视图和异常提示。
- **建议下一步**：先基于已稳定的 Real 模式流程产出视觉稿和页面验收清单，再分页面替换，不在硬件联调期间大改业务逻辑。

### P2-07｜完成生产账户、密钥、本机配置与主机安全加固

- **目标**：消除开发环境默认账号、明文本机配置和不受控工程权限在现场部署中的风险。
- **当前状态**：开发环境账号/本机配置机制存在；生产身份策略、首次改密、密钥保管和现场主机加固尚未完成或未验证。
- **前置条件**：现场账户、医院/设备方安全要求和部署模式明确。
- **完成标准**：
  - 生产环境禁用开发默认密码与示例凭据；具备首次改密、强密码、账户停用和最小权限策略。
  - LIS、SDK 路径、端口、服务账户和其他敏感配置均不进入 Git、日志、UI 或导出文件。
  - 工程二次认证、审计、会话超时和本机管理权限符合现场要求。
  - Windows 主机、服务账户、目录权限、USB/串口访问和日志/备份目录权限完成最小权限配置。
  - 完成生产环境安全检查表与验收记录。
- **建议下一步**：在部署前先列出所有账户、密钥、配置文件和目录权限，再逐项确认归属与保管方式。

### P2-08｜完成项目文档一致性、需求追溯与旧原型清理标识

- **目标**：确保正式文档、接口说明、SOP、测试记录和仓库入口与当前 .NET 9 正式系统一致，避免旧 FastAPI/Mock 描述误导后续维护。
- **当前状态**：已形成项目上下文、TODO、Mock 验收和部分工程文档；旧 `src/` 与 `stainer_twin_fastapi/` 原型目录已于 `6b67b55` 删除。根目录 `README.md` 已同步当前 HEAD、`/control-console` 唯一入口、当前失败回归、Bridge 离线自检和 Real/Mock 边界，并移除对已删除 `wwwroot/static/*` 的验证命令；其余历史文档仍需持续核对。
- **前置条件**：P1 关键 Real 能力和 P2 部署方案稳定后逐步收口。
- **完成标准**：
  - [x] 根目录 README 已明确正式 Host、数据库、运行方式、Mock/Real 边界和文档索引；后续提交改变这些事实时须同步更新。
  - 当前入口不再引用已删除的 `src/` / `stainer_twin_fastapi/`；确需保留的旧 Mock/legacy 说明必须明确标识为历史参考。
  - 需求条目、设计决策、接口参数、测试证据、SOP 和验收结果建立可追溯关系。
  - 所有文档明确区分“已在真实设备验证”“仅 Mock 验证”“待现场确认”。
  - 文档不包含厂商二进制、真实凭据、绝对敏感路径或未验证推测。
- **建议下一步**：先核对正式入口 README、真实设备接口清单、PROJECT_CONTEXT.md、TODO.md 和验收文档之间的状态一致性，再清理或标注旧文档。
